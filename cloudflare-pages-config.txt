# Cloudflare Pages 部署配置

# ============ 构建命令 ============
# npm run build

# ============ 输出目录 ============
# dist

# ============ 环境变量（在 Cloudflare Pages Dashboard 设置） ============
# 必填：
#   DEEPSEEK_API_KEY=你的DeepSeek密钥
#
# 可选：
#   VITE_SUPABASE_URL=...
#   VITE_SUPABASE_ANON_KEY=...

# ============ API 端点（自动由 /functions 目录提供） ============
# POST /api/analyze-public → functions/api/analyze-public.ts
